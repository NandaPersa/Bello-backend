generator client {
  provider = "prisma-client-js"
  output   = "../../../../../node_modules/@prisma/client-scheduling"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_SCHEDULING")
}
