generator client {
  provider = "prisma-client-js"
  output   = "../../../../../node_modules/@prisma/client-payments"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_PAYMENTS")
}
