generator client {
  provider = "prisma-client-js"
  output   = "../../../../../node_modules/@prisma/client-client-records"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_CLIENT_RECORDS")
}
